//objeto math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); //redondeo
resultado = Math.ceil(2.1); //redondea hacia arriba
resultado = Math.floor(2.9); // redondeo hacuia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-123); // numero psitivo
resultado = Math.min(123, 32, 4, 5, 234); // min
resultado = Math.max(123, 5345, 5, 434); // max
resultado = Math.random(); // max

resultado = Math.floor(Math.random() * 30);

console.log(resultado);
